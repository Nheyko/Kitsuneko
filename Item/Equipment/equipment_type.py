from enum import Enum

Equipment_type = Enum('Equipment_type', ['NONE',
                                         'WEAPON',
                                         'ARMOR',
                                         'SHIELD'])